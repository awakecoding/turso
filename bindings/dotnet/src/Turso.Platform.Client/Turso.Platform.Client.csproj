<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Library</OutputType>
    <TargetFrameworks>$(TursoTargetFrameworks)</TargetFrameworks>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Turso.Platform.Client</RootNamespace>

    <PackageId>Turso.Platform.Client</PackageId>
    <Title>Turso Platform API Client</Title>
    <Description>HTTP client for the Turso Platform API (api.turso.tech): manage databases, groups, auth tokens, organizations and members. Generated from the official OpenAPI specification.</Description>
    <Authors>Turso authors</Authors>
    <PackageTags>turso;database;platform;api;client</PackageTags>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageProjectUrl>https://github.com/tursodatabase/turso</PackageProjectUrl>
    <RepositoryUrl>https://github.com/tursodatabase/turso</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>$(NoWarn);1591</NoWarn>

    <PlatformClientIntermediateOutputPath>$([System.IO.Path]::Combine('$(BaseIntermediateOutputPath)', '$(Configuration)', '$(TargetFramework)'))/</PlatformClientIntermediateOutputPath>
    <TransformedOpenApiDocument>$(PlatformClientIntermediateOutputPath)openapi.transformed.json</TransformedOpenApiDocument>
    <GeneratedClientSource>$(PlatformClientIntermediateOutputPath)TursoPlatformClient.generated.cs</GeneratedClientSource>
    <OpenApiTitlerBaseIntermediateOutputPath>$(PlatformClientIntermediateOutputPath)OpenApiTitler/obj/</OpenApiTitlerBaseIntermediateOutputPath>
    <OpenApiTitlerBaseOutputPath>$(PlatformClientIntermediateOutputPath)OpenApiTitler/bin/</OpenApiTitlerBaseOutputPath>
  </PropertyGroup>

  <ItemGroup>
    <!-- Keep generated and tool artifacts out of the client when intermediates are redirected. -->
    <Compile Remove="artifacts/**;obj/**;tools/**" />
    <None Remove="tools/**" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="NSwag.MSBuild" Version="14.7.1" PrivateAssets="all" />
    <Compile Include="$(GeneratedClientSource)" Visible="false" />
  </ItemGroup>

  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="README.md" />
    <None Include="openapi.json" />
    <None Include="nswag.json" />
  </ItemGroup>

  <Target Name="GeneratePlatformClient"
          BeforeTargets="CoreCompile"
          Inputs="$(MSBuildProjectFullPath);openapi.json;nswag.json;tools/OpenApiTitler/Program.cs;tools/OpenApiTitler/OpenApiTitler.csproj"
          Outputs="$(TransformedOpenApiDocument);$(GeneratedClientSource)">
    <MakeDir Directories="$(PlatformClientIntermediateOutputPath)" />
    <Exec WorkingDirectory="$(MSBuildProjectDirectory)" Command="dotnet run --project &quot;$(MSBuildProjectDirectory)/tools/OpenApiTitler/OpenApiTitler.csproj&quot; --configuration $(Configuration) --no-launch-profile -p:BaseIntermediateOutputPath=&quot;$(OpenApiTitlerBaseIntermediateOutputPath)&quot; -p:BaseOutputPath=&quot;$(OpenApiTitlerBaseOutputPath)&quot; -- &quot;$(MSBuildProjectDirectory)/openapi.json&quot; &quot;$(TransformedOpenApiDocument)&quot;" />
    <Exec Command="$(NSwagExe_Net100) run &quot;$(MSBuildProjectDirectory)/nswag.json&quot; &quot;/variables:OpenApiDocument=$(TransformedOpenApiDocument),GeneratedClientSource=$(GeneratedClientSource)&quot;" />
  </Target>

  <Target Name="CleanGeneratedPlatformClient" AfterTargets="CoreClean">
    <Delete Files="$(TransformedOpenApiDocument);$(GeneratedClientSource)" />
  </Target>
</Project>
